{
  "domain": "conventions",
  "references": [
    {
      "id": "conv-001",
      "name": "mindmap_display_layout",
      "description": "Mermaid mindmap uses radial auto-layout. Normal mode: session node expanded with children, plus all other top-level groups (work, documentation, architecture, constraints, conventions) as collapsed leaf nodes. Full mode: all nodes expanded with children. No left-right control available in mermaid mindmap type.",
      "far_memory_refs": [29, 30],
      "mind_memory_refs": ["knowledge::conventions::mindmap_display_layout"]
    },
    {
      "id": "conv-002",
      "name": "theme_default",
      "description": "Use mermaid default theme for mindmap rendering. Always include %%{init: {'theme': 'default'}}%% directive in output. This provides automatic multi-color nodes per branch depth. Custom blue theme is not supported by mermaid mindmap type.",
      "far_memory_refs": [31, 32],
      "mind_memory_refs": ["knowledge::conventions::theme_default"]
    }
    ,
    {
      "id": "conv-003",
      "name": "far_memory_topic_splitting",
      "description": "When far_memory.json grows large, Claude splits it into topic-based archive files. Each archive groups messages by summarized subject (from near_memory summaries). Main far_memory.json keeps only the active conversation topic. Archive files are stored in sessions/archives/ named by topic slug (e.g., far_memory_architecture_design.json). Main file maintains an 'archives' index array with {file, topic, message_range, near_memory_range}. On split, Claude identifies topic boundaries from near_memory summary clusters, extracts completed topics to archive files, and updates the index. mind-context default view shows the last active conversation from near_memory.",
      "far_memory_refs": [47, 48],
      "mind_memory_refs": ["knowledge::conventions::far_memory_topic_splitting"]
    },
    {
      "id": "conv-004",
      "name": "programs_over_improvisation",
      "description": "Claude-as-engine is bootstrap only (new session, resume, compaction recovery). All mechanical operations use Python scripts in scripts/. Claude provides intelligence (summaries, topic names) as arguments to deterministic programs. When architecture evolves, scripts must be updated to match — programs ARE the implementation of the architecture.",
      "far_memory_refs": [51, 52, 55, 56],
      "mind_memory_refs": ["knowledge::architecture::programs_over_improvisation", "knowledge::conventions::programs_over_improvisation"]
    },
    {
      "id": "conv-005",
      "name": "node_text_rules",
      "description": "Mindmap node text must be human-readable descriptions only. No raw IDs, internal pointers, or inline refs in node labels. Avoid mermaid reserved words (e.g. 'mindmap', 'graph', 'flowchart') in node text as they cause syntax errors.",
      "far_memory_refs": [],
      "mind_memory_refs": ["knowledge::conventions::node_text_rules"]
    }
  ],
  "external_files": []
}
