# Session Notes — 2026-02-25

## Session context
- Branch: claude/general-session-OGp5l
- Elevated: GH_TOKEN set, gh_helper.py active
- Mode: Autonomous

## Work completed

### PR #291 — DOCX Export Bug Fixes (6 bugs)
Fixed all 6 confirmed bugs in the HTML-to-Word blob export:

**Bug 1 — MSO header/footer rendered as page content**
- Added `display:none` to all 4 MSO element divs in publication.html
- Word desktop processes `mso-element` regardless; LibreOffice/Word Online now hide them

**Bug 2 — Cover page appeared twice**
- `container.cloneNode(true)` already includes `#pub-cover-page`
- Removed live DOM re-clone + `insertBefore` pattern
- Now uses `clone.querySelector('#pub-cover-page')` to show cover in place

**Bug 3 — Too many page breaks**
- Removed `page-break-before:always` from h2 CSS rule
- Applied via JS loop only for h2 elements at index > 0 (first h2 flows after cover)

**Bug 4 — Pie chart not visible**
- Changed `.pie-inline` to `[class*="pie-"]` for robust matching

**Bug 5 — Mermaid Gantt missing**
- Added render-wait guard: checks `.mermaid:not(:has(svg))`, shows toast, waits 2.5s

**Bug 6 — Stories #6/#7 missing from Delivery Calendar Gantt**
- Added `section Export & Documentation` with #6 and #7
- Updated EN web page, FR web page, and source README

### PR #292 — Stranded Work Recovery
Recovered 2 non-empty commits from `address-pending-issues-9vim4` branch:
- `docs: revert #0 TOCs to link/indent format` — Knowledge System TOC reverted in 4 files
- `v48: plan-mode-aware wakeup` — CLAUDE.md v48 + satellite-commands.md updated

### PRs #294–#304 — DOCX Export QA (10 fixes)
Complete DOCX export pipeline brought to production-ready state:
- PR #294: MSO header/footer placement + mso-title-page
- PR #295: async Mermaid PNG, cover page break, MSO div cleanup
- PR #296: inject explicit page-break paragraph after cover
- PR #297: Word-compatible cover page layout — margin-top, bordered rule
- PR #298: visual fidelity — pie charts, emoji color, story-row layout
- PR #299: story table — no header, consistent row borders
- PR #300: html-docx-js OOXML + emoji alignment + borderless inner tables
- PR #302: restore running header/footer via JSZip post-processing
- PR #303: remove invalid style refs, null-check ZIP entries, lastIndexOf sectPr
- PR #304: **FINAL** — OOXML ZIP from scratch via altChunk (removes html-docx-js dependency)

### Documentation Sprint — P6 Export Documentation + P8 Documentation System
Full documentation of all 2-day work across both projects:
- **NEWS.md**: 11 new entries for P6, 4 new entries for P8, updated By Type/Structure/Quality sections
- **P6 project metadata**: Updated with 5 tasks (T1–T5), full activity timeline (37 PRs), evolution log
- **P8 project metadata**: Updated with 3 tasks (T1–T3), activity timeline, 3 convention layouts reference
- **GitHub Board #10 (Export Documentation)**: 10 items total, all Done
- **GitHub Board #38 (Documentation System)**: 5 items total, all Done
- **Issue #305**: Combined task tracking all 2-day work, linked to both boards
- **Issue #301**: Closed with summary (all 8 DOCX fixes completed)

## Status at session end
- main is current and clean
- All deliveries merged
- No pending checkpoint

## Remember
- remember harvest: DOCX export — `display:none` on MSO element divs is the correct cross-app technique. Word processes `mso-element` attribute regardless of display value; LibreOffice/Word Online respect `display:none` and hide the divs from page body.
- remember harvest: cover page double-clone pattern — `container.cloneNode(true)` already clones hidden divs in the layout before `{{ content }}`. Never re-clone from live DOM and insertBefore — use `clone.querySelector()` instead.
- remember harvest: h2 page breaks — never set `page-break-before:always` globally in CSS for DOCX export. Apply via JS loop starting at index 1 to spare the first h2 after the cover page.
