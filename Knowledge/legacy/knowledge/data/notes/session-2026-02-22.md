# Session 2026-02-22

## Scoped Notes

### #0 + #10: wolfssl/curl requirement for gh_helper.py
- `gh_helper.py` (to be created) must use ONLY curl compiled with wolfssl
- No system curl, no `gh` CLI, no Python `requests`
- On wolfcrypt/wolfssl test environments, all GitHub API communication goes through wolfssl-linked curl
- End-to-end wolfssl crypto stack — no mixed TLS implementations
- Applies to token-mediated API bypass (v28) and all REST API operations

### Crash pattern: core/satellite window swapping (unknown issue)
- Observed 2026-02-21 during intensive bootstrap/wakeup testing
- Pattern: two Claude Code desktop app instances open simultaneously (core + satellite)
- User switches focus between windows
- Satellite crashes first, then core crashes shortly after
- Happened repeatedly ("a lot") during the testing session
- Root cause unknown — could be:
  - Shared proxy resource contention
  - Beacon port collision (both on 21337/localhost)
  - Desktop app shared state / IPC issue
  - API-level crash cascade (related to Pitfall #12?)
- Status: tracked for investigation, no fix yet
