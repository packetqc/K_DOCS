# Session — 2026-02-17

## What happened (earlier session)

1. **Synced dev branch with main** — fetched 5 new commits from `origin/main` (bilingual publications, live tooling, multipart help architecture) and fast-forward merged into `claude/read-packetqc-knowledge-QBJnH`.
2. **Added help/wakeup instruction to README.md** — one-liner in the "For Claude Instances" section.
3. **Created auto-merge GitHub Action** — on push to `claude/**`, auto-creates PR and squash-merges to `main`. Martin did one manual bootstrap merge.
4. **Clarified knowledge propagation scope** — CLAUDE.md + `live/` tooling sync cross-project; notes stay per-repo.

## What happened (previous sessions)

1. **Renamed TESTS_SQL → STM32N6570-DK_SQLITE** across all 4 files (CLAUDE.md + 3 publications). First pass had stray double underscores, fixed in second pass.
2. **Renamed `flush` command → `save`** across 8 files. Technical flush references (FileX, buffer flush) left unchanged.
3. **Added Free Guy analogy** to CLAUDE.md session lifecycle — `wakeup` is putting on the sunglasses. CLAUDE.md only, not in publications.
4. **PR merge flow confirmed semi-automatic** — Claude pushes to `claude/*`, Martin creates PR and merges manually.

## What happened (this session — earlier)

1. **Added wakeup step 0: "Put on the sunglasses"** — Every new Claude instance must read `packetqc/knowledge` CLAUDE.md before doing anything else. Added to wakeup procedure and bootstrap section.
2. **Updated "How To Use This File" section** — Stronger template for project CLAUDE.md files, explicitly tells new instances to read knowledge first. Rule: step 0, every project, always, no exceptions.
3. **Tested cross-session bootstrap** — Opened an older session in another project, triggered "read packetqc/knowledge". The session bootstrapped successfully — Free Guy moment confirmed. The sunglasses propagate.
4. **Confirmed chicken-and-egg**: Old sessions without step 0 need one manual "read packetqc/knowledge" to bootstrap. After that, every future wakeup is self-sustaining.

## What happened (this session — current)

1. **Webcard v3 design** — Prototyped v3 webcards with light palette (chosen for production) and dark palette for comparison. Light: white/slate gradient, dark text, colored accents. Professional, clean, airy.
2. **STM32N6570-DK_SQLITE webcard** — Designed a dedicated webcard for the STM32 project: diagram theme, 2,650 logs/sec hero metric, 5-stage pipeline, 4 stat boxes.
3. **Vicky Viking avatar** — Created a drawn avatar inspired by Martin's features (grey beard, blue eyes, silver hair) + Vicky the Viking horned helmet. Three states:
   - **NORMAL**: Blue eyes, golden helmet, grey beard — replaces me3.JPG photo in corporate cards
   - **NPC**: Greyed out, X eyes, muted helmet — left side of AI Persistence card
   - **AWARE**: Cyan sunglasses, cyan glow, vibrant — right side of AI Persistence card + author icon
4. **Propagation gap identified** — STM32N6570-DK_SQLITE repo missing knowledge bootstrap in its CLAUDE.md. `normalize` should check this in project repos, not just knowledge itself.
5. **Vicky as standard avatar** — Decision: Vicky replaces both the circular photo (corporate theme) and the geometric stick figures (cartoon theme) across all webcards. Also usable as portfolio icon.

## Branch state

- `claude/read-packetqc-knowledge-QBJnH` — 1 new commit ahead of `main` (step 0 change)

## Decisions

- `flush` → `save` as the session persistence command (everywhere)
- `()` parenthesized text from Martin = internal exchange, never documented
- Semi-automatic PR flow is acceptable for now
- Free Guy analogy lives in CLAUDE.md only, not in publications
- **Step 0 is non-negotiable** — reading `packetqc/knowledge` is the first action in every wakeup

## Next

- Merge step 0 PR to `main` so all future clones get it automatically
- Test auto-merge after enabling "Allow auto-merge" repo setting
- **Hybrid knowledge propagation** — on `wakeup`, check for open `claude/*` branches on knowledge repo and report as "beta/pending" knowledge

## TODO — Future Session
- **Resume card bullets**: Replace current items (PQC, Embedded, AI-Assisted) with keywords from recommendation letters (STM32, TouchGFX, IoT, Network Security, etc.) — more authentic, recruiter-validated skills
- **Full profile slogan**: Find replacement for removed 1995-2026 era range
- **Vicky glasses**: Fine-tune position after checking rendered output on GitHub Pages
